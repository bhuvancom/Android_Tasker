<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".TaskDetailsAndManipulation  ">


            <item
                android:id="@+id/menu_edit_task"
                android:contentDescription="Edit this task"
                android:icon="@drawable/ic_edit_black_24dp"
                android:title="Edit"
                app:showAsAction="always"
                />
            <item
                android:id="@+id/menu_delete_task"
                android:contentDescription="Delete this task"
                android:icon="@drawable/ic_delete_black_24dp"
                android:title="Delete"
                app:showAsAction="always"
                />

            <item
                android:id="@+id/menu_remind_task"
                android:icon="@drawable/addtodo"
                android:title="Reminder"

                app:showAsAction="always"
                />

</menu>